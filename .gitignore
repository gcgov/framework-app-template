composer.phar
/vendor/
/.idea
version.json

# Local secrets — never commit real values (see DOCKER.md)
.env
.env.local
/secrets/
# per-environment variable overlays for gf db:*/env at the app root (prod.env.example stays committed)
/*.env

.phpunit.cache/
.phpunit.result.cache
.phpstan-cache/
